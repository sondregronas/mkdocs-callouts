from setuptools import setup

raise RuntimeError("Installation using setup.py is not supported, use 'pip install .' instead.")

# Fake reference so GitHub still considers it a real package.
setup(name="mkdocs-callouts")
